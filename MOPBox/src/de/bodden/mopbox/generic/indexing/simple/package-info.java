/**
 * The implementations of indexing strategies in this package are deliberately simplistic.
 * They use only strong references and therefore cannot be efficient in practice.
 * On the other hand, they have an almost 1:1 correspondence to the algorithms described
 * in the JavaMOP papers.
 */
package de.bodden.mopbox.generic.indexing.simple;